import { PlaceholderPage } from "@/components/placeholder-page";

export default function RolesSettingsPage() {
  return <PlaceholderPage eyebrow="Settings" title="Roles and permissions" description="Role definitions, branch scoping, approval defaults, and access boundaries." />;
}

